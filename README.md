#### 1) What is the difference between `null` and `undefined`?
#### ans:null means value of a variable is intentionallet set null of this has nothing and type of null is object.For undefined when a variable is declared but no value is not assingned or we use a variable before assingn in a variable it is undefined and type of undefined is undefined.

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
#### ans: map() is an array method and when we want to transfrom all the elements of an array we use map()method.Difference between map() and forEach is map returns a transfromed array of same length of original array but forEach is only iterate all elements of an array but it does not return any array.

#### 3) What is the difference between `==` and `===`?
#### ans: `==` compared only the value not the type of data.so for different types data coercion occurs when we used ==.`===` compared not only the value but also of the type of data.

#### 4) What is the significance of `async`/`await` in fetching API data?
#### ans:`async`/`await` improved readability of the code ande reduced nesting problem like callback hell.
#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
#### ans: Global scope:In global scope a variable can be used any where. Function scope:In function scope a variable can only used within this fucnction and outside this function those variable has no esistence . Block scope:In block scope a variable can only exists in a block`{}`.